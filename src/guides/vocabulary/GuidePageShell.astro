---
interface Props {
  page: number;
  total: number;
  label: string;
  bodyClass?: string;
}

const { page, total, label, bodyClass = "" } = Astro.props;
const ariaLabel = `Page ${page}: ${label}`;
---

<div class="page-frame">
  <section class:list={["page", bodyClass]} id={`page-${page}`} aria-label={ariaLabel}>
    <header class="page-header">
      <div class="brand">Word<span>smith</span></div>
    </header>
    <div class="page-body">
      <slot />
    </div>
    <footer class="page-footer">
      <span>Vocabulary &middot; 11+ word study</span>
      <span>{page} / {total}</span>
    </footer>
  </section>
</div>
